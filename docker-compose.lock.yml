# Generated by dependency lockfile implementation
# Date: 2025-04-08
# This file locks the Docker image versions used in the project
# Do not modify this file directly

version: "3.8"

# This is a reference file that documents the exact versions of Docker images
# used in the project. It is not meant to be used directly with docker-compose.

services:
  # LibreChat service
  librechat:
    image: librechat/librechat:0.6.9

  # API service (alias for librechat in some compose files)
  api:
    image: librechat/librechat:0.6.9

  # MongoDB service
  mongodb:
    image: mongo:6.0.12

  # LLM server service
  llm-server:
    image: ollama/ollama:0.1.27

  # MCP server service
  mcp-server:
    # Built from local Dockerfile
    build:
      context: ./services/mcp-server
      dockerfile: Dockerfile
    # No specific image tag as it's built locally
