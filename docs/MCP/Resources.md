# Resources in MCP

Servers expose resources using `resource://` URIs.

Supported methods:
- `resources/list`
- `resources/read`
- `resources/subscribe`

MCPgeek monitors that resource structure is valid and subscribable.